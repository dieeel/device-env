# dev

## base lang

### python

```
brew install uv
uv python list
uv python install 3.14
cat << 'EOF' >> ~/.zshrc

# uv python configuration
export PATH="$HOME/.local/bin:$PATH"
alias python='uv run python'
alias python3='uv run python'
EOF
source ~/.zshrc
```

for check

```
python -c "import sys; print(sys.executable)"
```

### mise

miseのインストール

```
brew install mise
```

zshrcに設定を追記（すでにある nodenv などの設定は削除してください）

```
echo 'eval "$(mise activate zsh)"' >> ~/.zshrc
source ~/.zshrc
```

### node.js

Node.js (LTS) のインストール

```
mise use --global node@24
```

> mise: node.jsのパッケージの管理／切り替え（n, nodenvから移行）

Corepackを有効化

```
corepack enable
```

pnpmを最新版に設定

```
corepack prepare pnpm@latest --activate
```

> pnpm ライブラリのパッケージ管理（npm, yarnから移行）

### java

```
mise ls-remote java
mise use --global java@openjdk-26
```

## container

### Podman

```
brew install podman
```

### docker

Colima（Dockerエンジン）のインストール

```
brew install colima
```

Docker CLI と Docker Compose のインストール

```
brew install docker docker-compose docker-credential-helper
```

## git

```
brew install --cask github
brew cask install sourcetree
```

### Finderのお気に入り

以下をショートカットに

* User名
* gitrepo

## Public Cloud

### OCI

```
brew install oci-cli
```

### aws

```
brew install awscli
```

### ssh-keygen

* bitbucket


## IDE

```
brew install visual-studio-code
brew install --cask antigravity
```

### VS code

必要であれば事前に、元のmacでインストール済み拡張機能のIDリストをテキスト保存

```
code --list-extensions > vscode-extensions.txt
```

テキストを読み込んで1つずつインストール

```
cat vscode-extensions.txt | xargs -L 1 code --install-extension
```

## etc

```
brew install jq
brew install automake cmake gcc
brew install tree
brew install plantuml
brew install gnu-tar
brew install zlib
brew install postman
```

